<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01//EN">
<html>
<head>
  <link rel="shortcut icon" href="http:///{{hostname}}/keedio.png">
  <title>Keedio's Tool Suite</title>
  <style type="text/css">
  body {
    padding-left: 11em;
    font-family: Georgia, "Times New Roman",
          Times, serif;
    color: #adcdc5;
    background-image: url("background.png");
   background-repeat:no-repeat;
   background-size:cover; } 
  ul.navbar {
    list-style-type: none;
    padding: 0;
    margin: 0;
    position: absolute;
    top: 2em;
    left: 1em;
    width: 9em }
  h1 {
    font-family: Helvetica, Geneva, Arial,
          SunSans-Regular, sans-serif }
  ul.navbar li {
    background: white;
    margin: 0.5em 0;
    padding: 0.3em;
    border-right: 1em solid black }
  ul.navbar a {
    text-decoration: none }
  a:link {
    color: #363066 }
  a:visited {
    color: #363066 }
  address {
    margin-top: 1em;
    padding-top: 1em;
    border-top: thin dotted }
  </style>
</head>

<body>

<!-- Site navigation menu -->
<ul class="navbar">
  <li><a href="http://{{hostname}}/ambari/" target="_blank">AMBARI</a>
  {% for ipa_server in ipa_server_hosts %}<li><a href="http://{{ hostname }}/ipa/ui/" target="_blank">FREEIPA{{ loop.index }}</a>{% endfor %}
  {% if has_hue_server %}<li><a href="http://{{ hostname }}/accounts/login/?next=/about/" target="_blank">HUE</a>{% endif %}
  {% if has_ganglia_server %}<li><a href="http://{{ hostname }}/ganglia" target="_blank">GANGLIA</a>{% endif %}
  {% for storm_ui in storm_ui_server_hosts %}<li><a href="http://{{ hostname }}/storm_{{ storm_ui }}" target="_blank">STORM{{ loop.index }}</a>{% endfor %}
  {% for nn in namenode_list %}<li><a href="http://{{ nn }}/dfshealth.jsp" target="_blank">HDFS NN{{ loop.index }}</a>{% endfor %}
</ul>

<!-- Main content -->
<h1>Keedio's Big Data Tool's Suite</h1>

<p>Keedio's tool suite home page!

<p>Home page with access to the web tools used by Keedio.&hellip;

<p>
<!-- Sign and date the page, it's only polite! -->
<address>Made 25 June 2015<br>
  by Keedio Team</address>

</body>
</html>
